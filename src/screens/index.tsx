import Splash from "./Splash";
import Login from "./auth/Login";

export const SCREENS = {
    Splash: {
        identifier: 'Splash',
        component: Splash
    },
    Login: {
        identifier: 'Login',
        component: Login
    }
}